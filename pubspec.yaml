name: mehrigiyo_crm
description: "Mehrigiyo CRM — Arizalar va boshqaruv tizimi"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  bloc_concurrency: ^0.2.5
  get_it: ^7.6.4
  injectable: ^2.3.2
  go_router: ^13.0.0
  dio: ^5.4.0
  intl: ^0.19.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  google_fonts: ^8.0.2
  shared_preferences: ^2.5.4
  web_socket_channel: ^3.0.1
  pretty_dio_logger: ^1.4.0
  web: ^1.1.0
  file_picker: ^8.0.0
  url_launcher: ^6.3.0
  just_audio: ^0.9.40
  just_audio_web: ^0.4.14
  flutter_colorpicker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.8
  injectable_generator: ^2.6.2
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
